apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  PORT: "3000"
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  JOB_QUEUE_NAME: jobs:queue
  PRIME_LIMIT: "100000"
  PRIME_LIMIT_MAX: "1000000"
