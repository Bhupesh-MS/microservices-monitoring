apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: stats-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: stats
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
